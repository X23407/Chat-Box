class dataReader{
    constructor(){
        this.data = {
            "Add" : {
                "start": "fruits = ['banana']<br>fruits.unshift('apple')",
                "Specific" : "data = ['an','the','on']<br>data.splice[index,0,newVal]",
                "last" : "data.splice(data.length,0,newVal)"
            }

        }
    }

}